---
phase: 08-transfer-portal
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - src/bot/storage/models.py
  - src/bot/storage/recruiting_store.py
  - src/bot/storage/cache.py
  - tests/test_recruiting_store.py
  - tests/test_cache.py
autonomous: true
requirements: [PORTAL-02, PORTAL-05, INFRA-01, INFRA-03]

must_haves:
  truths:
    - "PlayerEntry supports a type field with backward-compatible deserialization"
    - "RecruitingStore.add_player accepts a player_type parameter"
    - "RecruitingStore.list_players supports optional position filtering (case-insensitive)"
    - "TTLCache stores values with per-key TTL and returns None for expired entries"
    - "Existing JSON data without type field loads without errors (defaults to target)"
  artifacts:
    - path: "src/bot/storage/models.py"
      provides: "PlayerEntry with type field"
      contains: "type: str"
    - path: "src/bot/storage/recruiting_store.py"
      provides: "Extended add_player and list_players"
      contains: "player_type"
    - path: "src/bot/storage/cache.py"
      provides: "TTLCache class"
      exports: ["TTLCache", "CacheEntry"]
    - path: "tests/test_recruiting_store.py"
      provides: "Tests for type field and position filter"
      contains: "test_add_player_with_type"
    - path: "tests/test_cache.py"
      provides: "Tests for TTL cache"
      contains: "test_get_expired_returns_none"
  key_links:
    - from: "src/bot/storage/recruiting_store.py"
      to: "src/bot/storage/models.py"
      via: "PlayerEntry import"
      pattern: "from bot.storage.models import PlayerEntry"
---

<objective>
Extend the data model and storage layer to support transfer types (outgoing/target), position filtering, and build a standalone TTL cache module for future CFBD API integration.

Purpose: Foundation for Phase 8 command updates -- the data layer must support type-aware operations and position filtering before commands can be wired.
Output: Extended PlayerEntry, extended RecruitingStore, new TTLCache, comprehensive tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-transfer-portal/08-CONTEXT.md
@.planning/phases/08-transfer-portal/08-RESEARCH.md

<interfaces>
<!-- Current PlayerEntry from src/bot/storage/models.py -->
```python
@dataclass
class PlayerEntry:
    name: str
    position: str
    school: str
    stars: int  # 0 for unrated, 1-5 for rated
    added_at: str = field(default_factory=lambda: datetime.now(timezone.utc).isoformat())

    def to_dict(self) -> dict: ...
    @classmethod
    def from_dict(cls, data: dict) -> "PlayerEntry": ...
```

<!-- Current RecruitingStore from src/bot/storage/recruiting_store.py -->
```python
class RecruitingStore:
    def __init__(self, filepath: Path) -> None: ...
    def load(self) -> None: ...
    def save(self) -> None: ...
    def add_player(self, sport: str, name: str, position: str, school: str, stars: int) -> PlayerEntry | None: ...
    def remove_player(self, sport: str, name: str) -> tuple[PlayerEntry | None, list[str]]: ...
    def list_players(self, sport: str) -> list[PlayerEntry]: ...
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend PlayerEntry, RecruitingStore, and add TTLCache with TDD</name>
  <files>src/bot/storage/models.py, src/bot/storage/recruiting_store.py, src/bot/storage/cache.py, tests/test_recruiting_store.py, tests/test_cache.py</files>
  <read_first>
    - src/bot/storage/models.py
    - src/bot/storage/recruiting_store.py
    - tests/test_recruiting_store.py
    - .planning/phases/08-transfer-portal/08-RESEARCH.md
  </read_first>
  <behavior>
    PlayerEntry type field:
    - Test: PlayerEntry(name="X", position="QB", school="Bama", stars=4).type == "target" (default)
    - Test: PlayerEntry(name="X", position="QB", school="Bama", stars=4, type="outgoing").type == "outgoing"
    - Test: PlayerEntry.from_dict({"name":"X","position":"QB","school":"Bama","stars":4,"added_at":"2026-01-01T00:00:00+00:00"}) produces type="target" (backward compat -- no type key in dict)
    - Test: to_dict() includes "type" key
    - Test: roundtrip from_dict(entry.to_dict()) preserves type

    RecruitingStore.add_player with player_type:
    - Test: add_player("football", "John", "QB", "Bama", 4, player_type="outgoing") creates entry with type="outgoing"
    - Test: add_player("football", "Jane", "WR", "Ohio St", 3) creates entry with type="target" (default)
    - Test: add_player with duplicate name still returns None regardless of type

    RecruitingStore.list_players with position filter:
    - Test: list_players("football", position="QB") returns only QB players
    - Test: list_players("football", position="qb") returns QB players (case-insensitive)
    - Test: list_players("football", position="RB") returns empty list when no RB players exist
    - Test: list_players("football") with no position returns all players (existing behavior preserved)

    TTLCache:
    - Test: set("key", "value") then get("key") returns "value"
    - Test: get("nonexistent") returns None
    - Test: set with ttl=0.0 then get after brief wait returns None (expired)
    - Test: invalidate("key") removes entry, returns True; invalidate("missing") returns False
    - Test: clear() removes all entries
    - Test: default_ttl is used when ttl not specified in set()
  </behavior>
  <action>
    **RED phase:** Write failing tests first.

    In `tests/test_recruiting_store.py`, ADD new test functions (do not modify existing tests):
    - `test_add_player_with_type_outgoing` -- add with player_type="outgoing", assert entry.type == "outgoing"
    - `test_add_player_default_type_target` -- add without player_type, assert entry.type == "target"
    - `test_add_player_duplicate_with_different_type` -- add same name with different type, assert returns None
    - `test_list_players_position_filter` -- add QB and WR players, filter by "QB", assert only QB returned
    - `test_list_players_position_filter_case_insensitive` -- filter by "qb" (lowercase), assert QB player returned
    - `test_list_players_position_filter_no_match` -- filter by "RB" with no RB players, assert empty list
    - `test_player_entry_type_default` -- create PlayerEntry without type, assert type == "target"
    - `test_player_entry_type_explicit` -- create PlayerEntry with type="outgoing", assert type == "outgoing"
    - `test_player_entry_backward_compat_from_dict` -- from_dict with no "type" key, assert type == "target"
    - `test_player_entry_to_dict_includes_type` -- to_dict contains "type" key
    - `test_player_entry_roundtrip_type` -- from_dict(to_dict()) preserves type="outgoing"

    Create `tests/test_cache.py` with:
    - `test_set_and_get` -- set then get returns value
    - `test_get_missing_returns_none` -- get nonexistent key returns None
    - `test_expired_entry_returns_none` -- set with ttl=0.0, brief time.sleep(0.01), get returns None
    - `test_invalidate_existing` -- set key, invalidate returns True, get returns None
    - `test_invalidate_missing` -- invalidate nonexistent returns False
    - `test_clear` -- set multiple keys, clear(), get all returns None
    - `test_custom_ttl_overrides_default` -- TTLCache(default_ttl=0.0), set with ttl=60.0, get returns value

    **GREEN phase:** Implement the production code.

    In `src/bot/storage/models.py`:
    - Add `type: str = "target"` field AFTER the `added_at` field on PlayerEntry
    - Add `"type": self.type` to `to_dict()` return dict
    - `from_dict` needs no changes -- `cls(**data)` works because `type` has a default value, so old dicts without "type" key still deserialize

    In `src/bot/storage/recruiting_store.py`:
    - Change `add_player` signature to: `def add_player(self, sport: str, name: str, position: str, school: str, stars: int, player_type: str = "target") -> PlayerEntry | None:`
    - In `add_player` body, pass `type=player_type` to `PlayerEntry(...)` constructor
    - Change `list_players` signature to: `def list_players(self, sport: str, position: str | None = None) -> list[PlayerEntry]:`
    - In `list_players`, after getting players list, add: `if position: players = [p for p in players if p.position.lower() == position.lower()]`
    - Keep existing sort by position

    Create `src/bot/storage/cache.py` with exactly the TTLCache implementation from 08-RESEARCH.md Pattern 4:
    - `from __future__ import annotations`
    - `import time` and `from dataclasses import dataclass`
    - `CacheEntry` dataclass with `value: Any` and `expires_at: float`
    - `TTLCache` class with `__init__(default_ttl=900.0)`, `get(key)`, `set(key, value, ttl=None)`, `invalidate(key)`, `clear()`
    - Use `time.monotonic()` for expiration checks
    - Docstring: `"""In-memory TTL cache for API responses."""`
  </action>
  <verify>
    <automated>cd "C:/Users/quant/Documents/Discord Bot" && python -m pytest tests/test_recruiting_store.py tests/test_cache.py -x -v</automated>
  </verify>
  <acceptance_criteria>
    - src/bot/storage/models.py contains `type: str = "target"` after added_at field
    - src/bot/storage/models.py to_dict returns dict with "type" key
    - src/bot/storage/recruiting_store.py add_player signature contains `player_type: str = "target"`
    - src/bot/storage/recruiting_store.py add_player body contains `type=player_type`
    - src/bot/storage/recruiting_store.py list_players signature contains `position: str | None = None`
    - src/bot/storage/recruiting_store.py list_players body contains `p.position.lower() == position.lower()`
    - src/bot/storage/cache.py contains `class TTLCache`
    - src/bot/storage/cache.py contains `class CacheEntry`
    - src/bot/storage/cache.py contains `time.monotonic()`
    - src/bot/storage/cache.py contains `default_ttl: float = 900.0`
    - tests/test_cache.py contains `test_expired_entry_returns_none`
    - tests/test_recruiting_store.py contains `test_add_player_with_type_outgoing`
    - tests/test_recruiting_store.py contains `test_list_players_position_filter_case_insensitive`
    - `python -m pytest tests/test_recruiting_store.py tests/test_cache.py -x` exits 0
  </acceptance_criteria>
  <done>All existing tests still pass, all new tests pass, PlayerEntry supports type field with backward compat, RecruitingStore supports player_type and position filter, TTLCache is functional with TTL expiration</done>
</task>

</tasks>

<verification>
- `python -m pytest tests/test_recruiting_store.py tests/test_cache.py -x -v` -- all tests pass
- `python -c "from bot.storage.cache import TTLCache; c = TTLCache(); c.set('k','v'); print(c.get('k'))"` prints "v"
- `python -c "from bot.storage.models import PlayerEntry; p = PlayerEntry.from_dict({'name':'X','position':'QB','school':'KU','stars':3,'added_at':'2026-01-01'}); print(p.type)"` prints "target"
</verification>

<success_criteria>
- PlayerEntry.type field exists with "target" default
- Old JSON data without type key loads without errors
- add_player accepts player_type parameter
- list_players supports case-insensitive position filtering
- TTLCache module exists with get/set/invalidate/clear and TTL expiration
- All tests pass (existing and new)
</success_criteria>

<output>
After completion, create `.planning/phases/08-transfer-portal/08-01-SUMMARY.md`
</output>
